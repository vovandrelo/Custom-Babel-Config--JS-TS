// Конфигурационный файл позволяет указать, какие использовать Plugin-ы/Preset-ы и с какими настройками:
{
	// Настройка Preset-ов:
	"presets": [
		// В качестве значения свойства "preset" указывается массив, модержащий в себе необходимые preset-ы и их настройку.
		// Причём, порядок записи peset-ов имеет значение: последний в списке preset будет запущен первым, а первый - последним.
		
		// Preset Env - содержит все базовые плагины для преобразования ECMAScript 2015+ кода:
		[ "@babel/preset-env", { "useBuiltIns": "usage", "corejs": "3.6.5" } ],
		// Дополнительная настойка о поддерживаемых браузерах находится в package.json.

		// Preset Typescript - содержит базовые плагины для преобразования TS-кода в JS-код:
		[ "@babel/preset-typescript", {} ]
		// Имейте в виду, что Babel не выполняет проверку типов, вам все равно придется установить и использовать TypeScript.

		// Таким образом, при запуске babel ко всем обрабатываемым файлам сначала будет применён preset "@babel/preset-typescript,
		// а затем его результату работы - @babel/preset-env.
	],
	// Настройка Plugin-ов:
	"plugins": []
}